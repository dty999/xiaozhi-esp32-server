# 第七阶段：部署与运维

> **目标**：Docker 化部署，CI/CD 管线，自动化测试，性能优化。
> **验证标准**：`docker compose up` 一键启动全栈服务。

---

## 7.1 Docker

### 7.1.1 `Dockerfile`

**文件**：`apps/web/Dockerfile`

```dockerfile
FROM node:22-alpine AS base
RUN apk add --no-cache libc6-compat python3 make g++
WORKDIR /app

# 依赖层
FROM base AS deps
COPY package.json pnpm-lock.yaml ./
RUN corepack enable pnpm && pnpm install --frozen-lockfile --prod

# 构建层
FROM base AS builder
COPY . .
RUN corepack enable pnpm && pnpm install --frozen-lockfile
RUN pnpm build

# 运行层
FROM base AS runner
ENV NODE_ENV=production

# 复制 standalone 输出
COPY --from=builder /app/apps/web/.next/standalone ./
COPY --from=builder /app/apps/web/.next/static ./apps/web/.next/static
COPY --from=builder /app/apps/web/public ./apps/web/public

# 复制 VAD 模型
COPY --from=builder /app/models/silero_vad.onnx ./models/silero_vad.onnx

# 上传目录
RUN mkdir -p /app/uploadfile

EXPOSE 3000 8000 8003

CMD ["node", "apps/web/server.js"]
```

### 7.1.2 `docker-compose.yml`

**文件**：`docker-compose.yml`（根目录）

```yaml
version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    ports:
      - "3000:3000"
      - "8000:8000"
      - "8003:8003"
    environment:
      - DATABASE_URL=postgresql://xiaozhi:password@postgres:5432/xiaozhi_esp32_server
      - REDIS_URL=redis://redis:6379
      - JWT_SECRET=${JWT_SECRET:-change-me-in-production}
      - NODE_ENV=production
    volumes:
      - upload_data:/app/uploadfile
      - ./models:/app/models:ro
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped

  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: xiaozhi_esp32_server
      POSTGRES_USER: xiaozhi
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-password}
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U xiaozhi -d xiaozhi_esp32_server"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    restart: unless-stopped

volumes:
  pg_data:
  redis_data:
  upload_data:
```

### 7.1.3 环境变量

**文件**：`.env.example`（根目录）

```env
DATABASE_URL=postgresql://xiaozhi:password@localhost:5432/xiaozhi_esp32_server
REDIS_URL=redis://localhost:6379
JWT_SECRET=your-jwt-secret-change-me
POSTGRES_PASSWORD=password
```

### 7.1.4 健康检查端点

**文件**：`src/app/api/health/route.ts`

```typescript
import { NextResponse } from 'next/server';
import { prisma } from '@/lib/db';
import { cache } from '@/lib/redis';

export async function GET() {
  const checks: Record<string, string> = {};
  
  // 数据库检查
  try {
    await prisma.$queryRaw`SELECT 1`;
    checks.database = 'ok';
  } catch {
    checks.database = 'error';
  }

  // Redis 检查
  try {
    await cache.set('health', '1', 10);
    checks.redis = 'ok';
  } catch {
    checks.redis = 'error';
  }

  const allOk = Object.values(checks).every(v => v === 'ok');
  
  return NextResponse.json(
    { status: allOk ? 'ok' : 'degraded', checks },
    { status: allOk ? 200 : 503 }
  );
}
```

---

## 7.2 CI/CD

### 7.2.1 GitHub Actions CI

**文件**：`.github/workflows/ci.yml`

```yaml
name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: xiaozhi_test
          POSTGRES_USER: test
          POSTGRES_PASSWORD: test123
        ports:
          - 5432:5432
        options: --health-cmd="pg_isready" --health-interval=10s
    
    steps:
      - uses: actions/checkout@v4
      
      - uses: pnpm/action-setup@v4
        with:
          version: 9
      
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'
      
      - run: pnpm install --frozen-lockfile
      
      - name: Lint
        run: pnpm lint
      
      - name: Type check
        run: pnpm --filter web tsc --noEmit
      
      - name: Test
        run: pnpm test
        env:
          DATABASE_URL: postgresql://test:test123@localhost:5432/xiaozhi_test
```

### 7.2.2 部署 GitHub Action

**文件**：`.github/workflows/deploy.yml`

```yaml
name: Deploy

on:
  push:
    tags:
      - 'v*'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      
      - name: Build and push Docker image
        run: |
          docker build -f apps/web/Dockerfile -t xiaozhi-next:${{ github.ref_name }} .
          # 推送到 registry（需配置 secrets）
          # docker push registry.example.com/xiaozhi-next:${{ github.ref_name }}
      
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_KEY }}
          script: |
            cd /opt/xiaozhi-next
            docker compose pull
            docker compose up -d
```

---

## 7.3 测试

### 7.3.1 Vitest 配置

**文件**：`apps/web/vitest.config.ts`

```typescript
import { defineConfig } from 'vitest/config';
import path from 'path';

export default defineConfig({
  test: {
    environment: 'node',
    globals: true,
    setupFiles: ['./src/test/setup.ts'],
  },
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
});
```

### 7.3.2 测试示例：认证 API

**文件**：`src/test/api/auth.test.ts`

```typescript
import { describe, it, expect, beforeAll } from 'vitest';

describe('Auth API', () => {
  let token: string;

  it('GET /api/auth/captcha should return SVG', async () => {
    const res = await fetch('http://localhost:3000/api/auth/captcha');
    expect(res.status).toBe(200);
    expect(res.headers.get('content-type')).toContain('svg');
  });

  it('GET /api/auth/pub-config should return config', async () => {
    const res = await fetch('http://localhost:3000/api/auth/pub-config');
    const data = await res.json();
    expect(data.code).toBe(0);
    expect(data.data).toHaveProperty('sm2PublicKey');
  });

  it('POST /api/auth/login should fail with wrong credentials', async () => {
    const res = await fetch('http://localhost:3000/api/auth/login', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ username: 'test', password: 'wrong', captchaId: '' }),
    });
    const data = await res.json();
    expect(data.code).not.toBe(0);
  });
});
```

### 7.3.3 E2E 测试（Playwright）

**文件**：`e2e/login.spec.ts`

```typescript
import { test, expect } from '@playwright/test';

test('login flow', async ({ page }) => {
  await page.goto('http://localhost:3000/login');
  
  // 页面应显示登录表单
  await expect(page.getByPlaceholder('用户名')).toBeVisible();
  
  // 输入凭据
  await page.getByPlaceholder('用户名').fill('admin');
  await page.getByPlaceholder('密码').fill('test123');
  
  // 点击登录
  await page.getByRole('button', { name: '登录' }).click();
  
  // 应跳转到首页
  await page.waitForURL('**/home');
  await expect(page.getByText('智能体')).toBeVisible();
});
```

---

## 7.4 性能优化

### 7.4.1 Next.js 配置优化

```typescript
// next.config.ts 追加
const nextConfig: NextConfig = {
  // ...现有配置
  
  // API 响应缓存
  experimental: {
    serverComponentsExternalPackages: ['onnxruntime-node'],
    staleTimes: { dynamic: 30, static: 180 },
  },
  
  // 图像优化
  images: {
    formats: ['image/avif', 'image/webp'],
  },
  
  // 压缩
  compress: true,
};
```

### 7.4.2 Prisma 连接池

```typescript
// src/lib/db.ts 优化
export const prisma = new PrismaClient({
  datasources: {
    db: {
      url: process.env.DATABASE_URL,
    },
  },
  // 连接池：生产环境 20，开发环境 5
  connectionLimit: process.env.NODE_ENV === 'production' ? 20 : 5,
});
```

### 7.4.3 WebSocket 多进程

```typescript
// server.js — Cluster 模式（生产环境）
import cluster from 'cluster';
import { availableParallelism } from 'os';

if (cluster.isPrimary) {
  const numCPUs = availableParallelism();
  console.log(`Primary ${process.pid} starting ${numCPUs} workers`);
  
  for (let i = 0; i < numCPUs; i++) {
    cluster.fork();
  }
  
  cluster.on('exit', (worker) => {
    console.log(`Worker ${worker.process.pid} died, restarting...`);
    cluster.fork();
  });
} else {
  // Worker 进程启动 WebSocket 服务器
  startWebSocketServer();
  // 注意：不同 Worker 端口需区分或使用 SO_REUSEPORT
}
```

---

## 7.5 数据库迁移指南

### 从旧系统迁移

```bash
# 1. 备份数据库
pg_dump -U xiaozhi xiaozhi_esp32_server > backup_$(date +%Y%m%d).sql

# 2. 在新环境运行 Prisma Migrate
npx prisma migrate dev --name init

# 3. 运行种子脚本
npx prisma db seed

# 4. 验证数据一致性
# 检查表数量、行数、关键字段
```

---

## 7.6 P7 验证清单

- [ ] `docker compose up` 一键启动
- [ ] `GET /api/health` 返回 200
- [ ] `pnpm test` 全部通过
- [ ] Playwright E2E 测试通过
- [ ] CI Pipeline 正常运行
- [ ] 生产环境部署成功
- [ ] 压力测试：100 并发 WebSocket 连接稳定
